apiVersion: v1
kind: Service
metadata:
  name: mysql-user-external # 새로운 이름
spec:
  type: NodePort # 외부 접근을 위한 NodePort
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30302 # Kafka VM이 접근할 포트
  selector:
    app: mysql-user # 동일한 StatefulSet Pod를 바라봄