apiVersion: v1
kind: Service
metadata:
  name: portfolio-service # 게이트웨이가 참조할 DNS 이름
spec:
  selector:
    app: portfolio-pod-label
  ports:
    - port: 80 # 게이트웨이가 바라볼 Service 포트
      targetPort: 8082 # Pod의 실제 포트