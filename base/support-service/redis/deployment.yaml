apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-support-deployment
  namespace: linkfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-support
  template:
    metadata:
      labels:
        app: redis-support
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379