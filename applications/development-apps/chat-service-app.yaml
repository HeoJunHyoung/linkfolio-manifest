apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: linkfolio-chat-service
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: 'https://github.com/HeoJunHyoung/linkfolio-manifest'
    targetRevision: feature/unified-infra-app-of-apps
    path: overlays/development/chat-service
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: linkfolio
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true